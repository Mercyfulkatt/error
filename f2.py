import math

x=0.5
h=0.1

def operacion(x):
     return  resu=0.2+200*math.pow(x,2)+675*math.pow(x,3)-900*math.pow(x,4)+400*math.pow(x,5)

    resul = operacion(0.5)
    res=0.5-0.25
    resultado = operacion (res)

    result=resul-resultado/0.1

    print (result)
